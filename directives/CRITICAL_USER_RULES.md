# CRITICAL PROJECT RULES

**Rule 1: ABSOLUTE DEPLOYMENT CONTROL**

- **NEVER merge any branch to `main` without explicit, written permission from
  the user.**
- **NEVER deploy to production without explicit, written permission from the
  user.**

This rule applies to the **entire workspace** and **all tasks**, regardless of
context. Even if a task says "deploy", you must ask for confirmation first.
